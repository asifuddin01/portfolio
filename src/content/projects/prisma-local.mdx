---
order: 3
title: "PRISMA-Local — screening a systematic review on your own hardware"
summary: "A systematic review begins with thousands of database records that two people screen by hand, and recall has to be near-total because one missed study invalidates the review. So the useful output is a ranking with a stopping rule, not a classifier — and because records often cannot leave the institution, it has to run locally. Small open models are fine-tuned to score a title and abstract against the protocol in a single generated token, and the score is the raw logit(INCLUDE) − logit(EXCLUDE) margin, which turns a classifier into a ranker. QLoRA takes a 7B onto 16GB, LoRA trains two independent 3B screeners, vLLM serves both adapters off one base copy with prefix caching. Trained on 67 of SYNERGY's 73 reviews and tested on 6 held out entirely, at each review's natural prevalence — 13,665 records, 4.6% includes, cross-topic with zero labels from the review being screened. It works and it is not good enough: WSS@95% 0.331 against 0.129 zero-shot, level with a TF-IDF logistic regression on the same reviews, and half of what within-review active learning reaches. Both baselines are computed here so the gap is stated rather than found by a reviewer. The finding worth keeping is a precision bug: taking the decision through a bf16 softmax collapsed 13,665 records onto 45 distinct scores, and recomputing the same margin in fp32 gave 13,619 — AUC moved by 0.002 while recall@10% moved by 26%. The metric that survived the bug is the one screening papers lead with, so score resolution is now logged for every run and a collapsed run is treated as broken rather than as a weak model."
cvSummary: "Local LLM screening assistant for systematic reviews (PyTorch, PEFT, vLLM, Gradio). QLoRA 4-bit fine-tune of Qwen2.5-7B and LoRA fine-tunes of Qwen2.5-3B scoring title+abstract against a protocol in one token, ranked by logit margin; trained on 67 SYNERGY reviews, evaluated on 6 held out at natural prevalence (13,665 records, 4.6% positive). Cross-topic transfer with zero in-review labels: WSS@95% 0.331 vs 0.129 zero-shot, reported level with a TF-IDF baseline and below active learning, both computed rather than cited. Diagnosed a bf16 softmax collapsing 13,665 records onto 45 distinct scores — recall@10% +26% after an fp32 fix that moved AUC by 0.002 — and made score resolution a logged, failing condition. vLLM prefix caching 1.40x. In-browser demo, RIS in and RIS out."
link: "https://asifuddin01.github.io/prisma-local/"
linkLabel: "Open the demo"
repo: "https://github.com/asifuddin01/prisma-local"
system: true
---
