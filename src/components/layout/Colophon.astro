---
import LesionRule from '../ornament/LesionRule.astro';
import { getEntry, render } from 'astro:content';
import { getContact } from '../../lib/collections';

const { email, github, linkedin } = await getContact();

const entry = await getEntry('site', 'colophon');
const Colophon = entry ? (await render(entry)).Content : null;
---
<footer class="cp">
  <div class="codex">
    <LesionRule />
    <div class="cp-body" data-reveal>
      <div class="cp-text">{Colophon && <Colophon />}</div>
      <ul class="cp-links">
        <li><a class="link" href={`mailto:${email}`}>{email}</a></li>
        <li><a class="link" href={github} rel="me">github</a></li>
        <li><a class="link" href={linkedin} rel="me">linkedin</a></li>
        <li>
          <a class="link" href="/vitae/cv">curriculum vitae</a>
        </li>
        <li><a class="link" href="/imagines">imagines</a></li>
      </ul>
    </div>
  </div>
</footer>

<style>
  .cp { padding-bottom: 4.5rem; }
  .cp-body {
    display: flex;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
    align-items: flex-start;
  }
  .cp-text :global(p) { margin: 0; }
  .cp-text {
    font-family: var(--font-mono);
    font-size: var(--t-xs);
    line-height: 1.9;
    letter-spacing: 0.03em;
    color: var(--ink-soft);
    max-width: 46ch;
    margin: 0;
  }
  .cp-links {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    list-style: none;
    margin: 0;
    padding: 0;
    font-family: var(--font-mono);
    font-size: var(--t-xs);
    letter-spacing: 0.03em;
  }
</style>
