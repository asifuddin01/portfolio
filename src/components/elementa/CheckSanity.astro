---
/**
 * A second, independent reason to believe the answer: shapes conform, rows
 * sum to one, the limit matches a known case, the sign is right.
 */
---
<div class="pb-slot">
  <p class="pb-label">Check — sanity</p>
  <slot />
</div>
