---
/**
 * An exercise's solution. `<details>`-based, so it works without JavaScript,
 * prints expanded and is indexed by search. Elementa has no answer-less
 * exercises: a hidden solution is a solution, a missing one is an
 * abandonment (§5.5).
 */
---
<details>
  <summary>Solution</summary>
  <slot />
</details>
